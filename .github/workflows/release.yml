name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish, without the leading v'
        required: true

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}
      minecraft: ${{ steps.meta.outputs.minecraft }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - uses: gradle/actions/wrapper-validation@v4

      # Tag builds are not on the default branch, where setup-gradle would default to
      # read-only, so the dependency cache would never be written for a release.
      - uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false

      # setup-gradle only caches the paths it knows; Loom keeps the Minecraft jars,
      # mappings and remapped dependencies outside them, and they dominate a cold build.
      - name: Cache Loom artifacts
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches/fabric-loom
            fabric/.gradle/loom-cache
          key: loom-${{ runner.os }}-${{ hashFiles('gradle/libs.versions.toml', 'fabric/build.gradle.kts') }}
          restore-keys: |
            loom-${{ runner.os }}-

      - name: Resolve version and Minecraft target
        id: meta
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            version="${{ inputs.version }}"
          else
            version="${GITHUB_REF_NAME#v}"
          fi
          minecraft=$(sed -n 's/^minecraft *= *"\(.*\)"$/\1/p' gradle/libs.versions.toml)
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "minecraft=$minecraft" >> "$GITHUB_OUTPUT"

      - name: Build and test
        run: ./gradlew build -PbuildVersion=${{ steps.meta.outputs.version }} --stacktrace

      - uses: actions/upload-artifact@v4
        with:
          name: LetsTroll-paper
          path: paper/build/libs/LetsTroll-paper-*.jar
          if-no-files-found: error

      - uses: actions/upload-artifact@v4
        with:
          name: LetsTroll-fabric
          path: fabric/build/libs/LetsTroll-fabric-*.jar
          if-no-files-found: error

  modrinth:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # A new loader is one entry here plus its Gradle module and an upload step above.
        include:
          - artifact: LetsTroll-paper
            loaders: |
              paper
              purpur
              folia
            dependencies: ''
          - artifact: LetsTroll-fabric
            loaders: |
              fabric
              quilt
            # The mod hard-depends on Fabric API; without this the server refuses to launch
            # and Modrinth would not tell anyone to install it.
            dependencies: |
              fabric-api(required)
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist

      - name: Publish to Modrinth
        uses: Kir-Antipov/mc-publish@v3.3
        with:
          modrinth-id: ${{ vars.MODRINTH_PROJECT_ID || 'lets-troll' }}
          modrinth-token: ${{ secrets.MODRINTH_TOKEN }}
          files: dist/*.jar
          name: LetsTroll ${{ needs.build.outputs.version }}
          version: ${{ needs.build.outputs.version }}
          version-type: release
          loaders: ${{ matrix.loaders }}
          dependencies: ${{ matrix.dependencies }}
          game-versions: ${{ needs.build.outputs.minecraft }}
          java: 21
          # Unfeaturing older versions needs a token that may edit versions;
          # the release token is only allowed to create them.
          modrinth-unfeature-mode: none

  github-release:
    needs: [build, modrinth]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: LetsTroll-*
          merge-multiple: true
          path: dist

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: LetsTroll ${{ needs.build.outputs.version }}
          generate_release_notes: true
          files: dist/*.jar
